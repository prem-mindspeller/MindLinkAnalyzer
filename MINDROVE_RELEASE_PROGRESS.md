# MindRove release progress

- 2026-08-07: Started verification to merge the current branch into `origin/mindrove-integration` and publish the matching MindRove release tag. This tracking file is excluded from the release change.
- 2026-08-07: Verified `origin/mindrove-integration` is still at `95009eb` and `n100.0.17-mindrove` is unused. The branches have diverged, so an explicit merge is required; the release workflow accepts `n*-mindrove` tags.
- 2026-08-07: Began the merge. Conflicts require reconciliation in `TaskSelection.jsx`, `loginService.js`, `taskQualityGate.mjs`, and `newBackend/main.py`; the other changes merged automatically.
- 2026-08-07: Resolved all merge conflicts while retaining the optimized battery, booking-count session gate, and MindRove signal-quality path. Focused frontend and backend tests passed, as did the production frontend build. Generated bytecode remains outside the release commit.
- 2026-08-07: Created merge commit `9d61546`, pushed it to `origin/mindrove-integration`, and pushed annotated release tag `n100.0.17-mindrove`. Remote branch and tag refs were verified; workflow execution was not inspected because `gh` is unavailable locally.
- 2026-08-07: Removed the development-only download-report fallback from the upload page. Analysis and upload to Mindspeller remain unchanged.
- 2026-08-07: Published the upload-only change as `n100.0.18-mindrove`. The annotated tag and `origin/mindrove-integration` both resolve to `d389514`.
- 2026-08-07: Started porting the committed MindRove integration changes to `feature/neuroprofile-traceability-v7` so the single-channel MindLink line receives the same shared application updates.
